title: Rockfish Toolkit
description: Suricata plugins and utilities — capture, IIoT/OT protocol parsers, per-flow telemetry. Powers the Rockfish NDR sensor.

# No `theme:` — we use a fully custom _layouts/default.html
# with embedded dark-mode-only styling.
plugins:
  - jekyll-seo-tag

show_downloads: false
github:
  is_project_page: true
